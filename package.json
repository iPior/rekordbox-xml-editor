{
  "name": "rekordbox-xml-editor-monorepo",
  "private": true,
  "version": "0.1.0",
  "packageManager": "bun@1.2.21",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:electron": "bun run --cwd apps/electron dev",
    "dev:tauri": "bun run --cwd apps/tauri tauri dev",
    "build:electron": "bun run --cwd apps/electron build",
    "build:tauri": "bun run --cwd apps/tauri tauri build",
    "typecheck": "bun run typecheck:packages && bun run typecheck:apps",
    "typecheck:packages": "bun run --cwd packages/types typecheck && bun run --cwd packages/core typecheck && bun run --cwd packages/ui typecheck",
    "typecheck:apps": "bun run --cwd apps/electron typecheck && bun run --cwd apps/tauri typecheck",
    "lint": "bun run typecheck"
  },
  "devDependencies": {
    "typescript": "^5.6.2"
  }
}
